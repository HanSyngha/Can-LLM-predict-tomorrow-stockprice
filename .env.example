# Stock-Self-Evolving Environment Variables
# Most settings are configured via the Dashboard Settings UI.
# These are only needed for advanced configuration.

# Server
PORT=4001

# Database (default: ./data/stock-evolving.db)
# DB_PATH=./data/stock-evolving.db

# Debug mode (enables verbose logging)
# DEBUG=true
